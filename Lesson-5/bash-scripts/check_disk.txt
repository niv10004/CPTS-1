What it does:

    Displays each mount point with:

        used space

        available space

        usage percentage

    Flags any mount that passes the threshold (default 80%) with a warning

    Filters out tmpfs and devtmpfs mounts for clarity
